//! Secrets Manager client CLI.
//!
//! Skeleton only — implemented in milestone M2
//! (`run` / `get` / `set` / `list` / `export`).

fn main() {
    eprintln!("secrets: client CLI not yet implemented (planned for M2)");
    std::process::exit(1);
}
